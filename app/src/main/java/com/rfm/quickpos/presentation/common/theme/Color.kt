package com.rfm.quickpos.presentation.common.theme

import androidx.compose.ui.graphics.Color

// Primary RFM Loyalty Colors
val RfmRed = Color(0xFFB31B1B) // Primary brand color from RFM Loyalty
val RfmGray = Color(0xFF5A5A5A) // Secondary brand color from RFM Loyalty

// Extended primary palette - lighter and darker variants
val RfmRedLight = Color(0xFFD64545)
val RfmRedDark = Color(0xFF8A0000)
val RfmGrayLight = Color(0xFF7E7E7E)
val RfmGrayDark = Color(0xFF333333)

// Accent colors
val RfmGreen = Color(0xFF00BF69) // For success states and action buttons
val RfmBlue = Color(0xFF0057B8) // For information and select states
val RfmYellow = Color(0xFFFFC107) // For warnings
val RfmOrange = Color(0xFFFF6B00) // For urgent actions

// Neutral colors
val RfmBlack = Color(0xFF121212)
val RfmDarkGray = Color(0xFF333333)
val RfmMediumGray = Color(0xFF999999)
val RfmLightGray = Color(0xFFE0E0E0)
val RfmOffWhite = Color(0xFFF5F5F5)
val RfmWhite = Color(0xFFFFFFFF)

// Semantic colors
val SuccessColor = RfmGreen
val ErrorColor = RfmRed
val WarningColor = RfmYellow
val InfoColor = RfmBlue
val DiscountColor = RfmGreen
val PendingColor = RfmOrange

// Surface colors
val SurfaceLight = RfmWhite
val SurfaceDark = Color(0xFF1D1D1D)
val SurfaceVariantLight = RfmOffWhite
val SurfaceVariantDark = Color(0xFF2D2D2D)

// Transparent colors
val TransparentRfmRed = Color(0x20B31B1B) // 12% opacity for red
val TransparentRfmGreen = Color(0x2000BF69) // 12% opacity for green